{
  "name": "bushido_school",
  "version": "1.0.0",
  "description": "Plataforma online de uma escola",
  "main": "backend/src/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon backend/src/server.js",
    "start:dev": "docker-compose up -d",
    "stop:dev": "docker-compose down",
    "start:all": "npm run start:dev && npm run start && cd backend && npm run db:start"
  },
  "author": "jorge de jesus cardoso",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mysql2": "3.0.0",
    "nodemon": "^3.1.0",
    "sequelize": "6.3.4"
  },
  "devDependencies": {
    "sequelize-cli": "6.6.1"
  }
}
